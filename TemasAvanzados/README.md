# Conceptos avanzados en Python

Este modulo profundiza en temas avanzados de Python, como:
* **Metodos de clase** 
* **Métodos mágicos**
* **Comparando objetos**
* **Comprensión de listas**
* **Función zip en Python**
* **Manejo de Cadenas** : función _split()_, función _find()_, función _replace()_, la multiplicación de cadenas y la función _strip()_
* **Funciones Lambda** 
* **Funciones _map()_, _filter()_ y _reduce()_ junto con lambda** 
* **Ordenamiento en Python con el método _sorted()_**
* **Generadores y expresiones generadoras**
* **Decoradores** entre ellos el decorador _property_ 
* **Contenedores**
* **Funcion _sum()_ y _next()_**
* **Manejo de excepciones**